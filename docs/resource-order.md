# 提案确认后的资源顺序一致性

更新：2026-09-10；固定点 `bc08cef`。本地修复手工提案、规划提案与资源采用共用写入器的顺序缺陷，不改变产品操作方式，也未托管发布。

## 行为与边界

用户明确确认的节点资源数组就是正式展示顺序。此前通用写入器把所有资源位置存为零，导致正式路径按资源 ID 重排、与已审阅提案及历史修订不一致。现在所有提案类型在同一写入核心中保存数组顺序，Web 与扩展读取一致，不再需要资源采用专属的排序后处理。

- 调整顺序保留绑定 ID；移除只归档旧绑定，已有学习会话仍指向原视频与节点。
- 未产生会话的绑定可以按既有规则移动节点；已关联会话的绑定跨节点移动仍整体拒绝，不能改写历史归属。
- 用户、版本、来源、有效期和不可变提案的保护保持不变。失败确认不留下部分归档或排序结果。
- 原样重放历史确认返回原应用版本，不恢复旧顺序，也不改写历史修订。
- 迁移只修复未来确认的写入行为，不回填现有位置。旧记录保持原样；下一次明确确认的新提案才保存其审阅顺序。

没有新增专门拖动排序／移除界面，不接受调用者自行传入资源 `position`，不新增公共写入接口。

## 实现与证据

- [增量迁移](../supabase/migrations/20260910122339_resource_order.sql)：仅修改共用写入核心与移除重复排序，核心仍为不可直接调用的 invoker 函数；既有 14 条迁移未修改。
- [公共 RPC 回归](../supabase/tests/resource_order.test.sql)：41 项新增检查覆盖手工、规划、扩展读取、顺序与修订一致性、移除／移动、历史重放、权限和原子失败；全库 743 项检查通过。
- [真实账号回归](../apps/web/agent-integration/resource-order.spec.ts)：实际本地 Auth／PostgREST／应用接口先复现 UUID 重排，再验证修复；全部账号集成 45 项通过。
- [已有数据升级](../scripts/test-m1-migration.mjs)：先生成旧错误顺序和待确认提案，验证迁移及历史重放逐行保留已有数据，未来确认修正顺序且保留会话。
- [生产页面旅程](../apps/web/resource-e2e/entry.spec.mts)：两主题实际确认流程通过；东方主题按逆 UUID 顺序保留两个已有资源，替换第二个后检查正式页面顺序与原视频会话，赛博验证新增。共 4 项启用模式检查通过；关闭模式 2 项通过、2 项执行旅程明确跳过。提供方仅回环 HTTP 夹具，没有真实消费。

完整 `check:m1` 通过：679 项应用测试、79 项 Edge 测试、四工作区类型检查、升级契约、向导、Next／WXT 构建和扩展安全检查；46 项默认生产浏览器回归通过，新增集成与浏览器测试另通过严格类型检查。主线程检查东方 320px 确认页截图，两个既有资源及替换后的顺序清楚可读；不是正式美术或完整无障碍验收。146 个相关本地文档链接有效，两类临时测试账号清理后为零。

Security／Performance Advisors 无问题，`public,private` 结构差异为空，本地迁移历史 15 项一致。修改函数无 lint 告警；五个既有其他函数的提示未扩改，不宣称全库 lint 零告警。主线程独立比较共用核心正文，仅有序号变量、数组序号遍历与位置赋值三处预期变化。

源码提交：`f9d9279`。

## 独立复核

### Standards

非作者审阅 `bc08cef...f9d9279` 全部十个文件：0 项确认规范违规、0 项可行动异味、0 项额外安全／正确性缺陷。独立函数正文比较确认核心仅三处排序修改、采用 guard 仅移除重复后处理，权限不变。完整函数替换是向前增量迁移所需，不要求修改旧迁移以减少行数。

### Spec

另一位非作者审阅同一范围：0 项确认遗漏、范围扩张或实现错误。核对手工／规划／采用、扩展读取、历史归属、失败回滚与升级不回填要求。两位复核均为独立源码及测试审阅，未冒称另行运行数据库或浏览器；运行结果由主线程验证。

## 发布与剩余事项

本地源码／数据库为 15 项迁移，托管仍为 7 项；没有部署、push、真实账号额度授予或费用。旧安装扩展的 Snapshot 2 升级安排仍是发布前置条件。

本修复关闭通用资源顺序缺陷，不代表 P7 完成。字幕保留／缓存合规、真实推荐质量、扩展学习工作台、正式双主题美术与托管双端验收继续依照[执行路线](execution-roadmap.md)推进。
