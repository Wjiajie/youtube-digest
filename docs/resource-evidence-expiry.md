# 资源证据的期限与到期恢复

2026-09-11 · P7 本地增量 · 固定点 `85f9fd0`。真实提供方继续默认关闭；本批实现内容生命周期保护，不构成字幕获取、保存、模型处理或再展示授权。

## 已实现的行为

服务器为明确配置了保留策略的账号创建资源运行。没有策略时拒绝新检索，不预留次数、不发起提供方请求。测试账号的策略只用于本地夹具，没有配置任何真实用户策略。

一次检索链只有一份可信来源起点与不可延长的内容期限。后续字幕读取、匹配和采用核验继承原期限；更新核验时间、重复读取、修改账号策略都不能给旧材料延期。执行的 120 秒租约和采用的 10 分钟确认窗口仍是不同概念。

到期后：

- RPC 读取、执行权申请、完成及采用确认停止使用旧正文，直接 Data API 读取也不能绕过限制。
- 清理覆盖同次检索的所有字幕／匹配副本和采用核验正文，拒绝未确认提案；保留最小终态回执与原运行 ID。
- 排队但未执行的次数只退回一次；已经取得执行权的不退款。迟到完成仍校验原执行权，不能复活正文或再生成提案。
- 正式蓝图、已确认修订、绑定、学习会话、笔记、成果和自评保持不变；精确重试已确认提案仍返回原修订。
- Web 到期时隐藏材料和后续操作，恢复标签页也检查期限。不会自动刷新、重新核验或再次消费；用户主动读取后看到“资源证据已到期清除”。

调用提供方前再次核对执行和内容期限；在途请求收到相应取消信号。这不保证提供方撤销已经收到的材料、停止计费或删除自己的副本。

旧版记录没有可信期限：升级保留原行，不推测授权、不回填日期、不在迁移中删除正文；RLS 和 RPC 停止暴露这些旧正文。持有原运行链接的用户仍能明确清除整条链，页面不需要先加载旧材料。已清理的历史回执继续可读。

## 顶层设计

沿用[主动清除](resource-evidence-clearing.md)的终态模型，用共享整链清理核心处理手动与自动原因。新元数据与业务回执分离，客户端不能提交或修改期限。采用前的来源检查独立验证期限，不把“结果曾经成功”当作“现在仍可使用”。

清理与 begin／claim／finish／apply 保持同一蓝图锁顺序。读取失败的事务可能回滚其中的惰性清理，因此不能把拒绝响应当作数据库已完成擦除的证明。硬期限访问限制与独立维护扫描必须同时存在。

私有维护入口按账号限批、跳过已锁住的蓝图，只返回处理账号数，不输出正文。账号策略只有数据库维护者可设置，Web、扩展与 worker 均不能读写。策略上限 25 日是工程保护上限，不是任何内容的授权期限；获许可后仍须按实际权利范围配置更严格的窗口。

浏览器展示保护不是 DRM 或全局撤回：已下载的响应、页面内存、离线副本及被修改的客户端时钟不能靠本地定时器可靠擦除。服务器仍是访问与执行的安全边界。

代码入口：[增量迁移](../supabase/migrations/20260910200329_resource_evidence_expiry.sql)、[可信期限读取与执行信号](../apps/web/src/lib/agent/resource-lifetime.ts)、[页面期限保护](../apps/web/src/app/resources/evidence-deadline.tsx)、[旧记录清理入口](../apps/web/src/app/resources/unmanaged-evidence.tsx)、[数据库行为检查](../supabase/tests/resource_expiry.test.sql)。

## 维护上线门槛

本地真实 pg_cron 已验证：用户不访问时也能调用限批扫描并清除到期夹具。测试任务及测试账号已移除，**当前没有长期本地或托管调度**；不能据此声称非活跃真实账号已经持续清理。

真实资源启用前必须完成：

1. 核清[内容与处理权限](resource-retention-research.md)，记录实际运营策略及责任人，不能以免费 API 额度代替许可。
2. 在目标数据库安装并验证持久维护调度，监控无正文的逾期数量、失败数和耗时；批次限额应与账号规模和清理时限匹配。
3. 演练任务停运、锁竞争、恢复后补清理；硬截止继续拒绝读取，不能失败时回退展示旧材料。
4. 完成托管增量迁移、Web／worker／扩展版本协调及真实账号验收，再开启执行。不要先部署要求新字段的读取器而继续使用旧数据库。

本地迁移目前 19 项；托管最后核实仍 7 项，本批没有重新访问或修改托管环境。已有旧正文不自动清理，需另行盘点并取得明确删除决定。

## 本地验收

先复现再修复：缺少策略仍会扣次、过期正文可直接读取、页面恢复后仍展示过期内容、执行权回执迟到后仍发起提供方请求。已有取消语义回归发现被误写成超时，修复后继续保留“主动取消”的原行为。

- 70 项新数据库检查及全部 1024 项 pgTAP 通过；升级逐行对照保留旧正文、回执、额度、租约和正式历史。
- 本地 schema diff 为空，19 项迁移历史对齐；安全／性能顾问无告警，数据库 lint 仍有五组既有函数告警。
- 62 项完整真实本地 Auth 集成通过，另补在途检索到期后不启动下游请求，资源集成 25 项通过。HTTP 提供方全部为回环夹具。
- 双主题生产构建浏览器 8 项通过，包括 320px 到期提示、手动读取回执、重新打开、无新增提供方请求及既有检索／匹配／采用／主动清理。主线程检查实际截图，无水平溢出或运行时错误。
- 新旧读取器和 UI 30 项通过；完整 `check:m1` 通过：940 项应用测试、79 项 Edge 检查、四工作区类型检查、迁移升级契约、向导和两端构建。扩展输出 645.79 kB，未增加权限或 3D 依赖；随后新增的 Edge 期限错误透传检查也通过。
- 双连接维护演练中，扫描在 457 ms 内跳过被锁住的账号、清理另一账号；解锁后处理剩余账号，再次扫描返回 0。两测试账号已精确清理，无长期任务。
- 关闭模式回归与两轴非作者复核结果在收口后补记。

复现使用 `scripts/with-m1-runtime.sh`：`npm run check:m1`、`npm run test:agent-runs`、`npm run test:resource-entry`、`supabase test db`。只允许既有本地 Supabase 与夹具账号，不使用真实提供方密钥。

未完成：持续托管调度、内容权利确认、真正刷新／下架识别、账号整体删除、处理商／日志／备份清理、恢复后的再删除，以及正式双主题 3D 的美术与性能门槛。完整 P1–P9 目标不因本切片通过而完成。
