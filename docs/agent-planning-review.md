# 规划记录与草案阅读

固定点 `9875833`，P6 用户阅读切片已本地实现，源码 `205a040`。用户可以从已保存的目标定义找到规划记录，恢复服务端保存的草案、查看周安排和依据、取消未完成运行；不重新调用模型、不直接改正式蓝图。

## 界面与权限契约

- 目标定义提供规划记录入口；记录列表按时间倒序、每页 20 项，仅取 ID 与日期。列表不使用历史表状态冒充当前状态，进入记录后用运行 RPC 核对新鲜度。
- 独立可收藏地址恢复单次运行。页面先验证真实账号再读取，所有交互绑定当前账号；换账号或身份失效时隐藏旧私有内容。未知 ID 与越权均不展示其他账号内容。
- 显示规划所依据的目标定义、来源修订、每周时间预算、假设、新建议路径与节点完成依据。已有目标与新增建议区分，不把整份蓝图快照中的旧目标称为新建议。
- queued／running 可取消并手动刷新；不轮询模型、不自动重试生成。断线时保留页面但注明不是最新状态；可重新读取。身份失败时不继续保留私人草案。
- ready 是已生成草案，不是已应用路径；stale 明确来源已变化，禁止宣称当前可应用。取消／超时说明不保证供应商免费，未知 token 不显示成零消费。
- 双主题共享内容与交互、不同容器和时间线表达；窄屏长文本不溢出，键盘与减少动态效果可用。不增加 3D 资产、分析正文事件或客户端持久缓存。

## 本批边界及验证

复用现有用户 RPC，读取／取消不创建服务端特权客户端或注入模型。服务端页面使用真实身份；交互通过绑定账号的 Server Actions，参照 [Next.js 服务端与客户端边界](https://nextjs.org/docs/app/getting-started/server-and-client-components)。测试 seam 是公开用户访问接口、React 交互，以及真实本地 Auth → 页面 → RPC 旅程；不模拟内部存储来宣称云端验收。

阅读批次没有新增付费模型配置、生成按钮或伪造成功示例。后续[提案确认增量](agent-planning-approval.md)已本地补齐运行转提案及确认时的双来源原子校验；真正发起规划、澄清对话和视频检索仍待实现。当前用户没有历史规划时展示诚实空状态。旧扩展升级安排之前不部署。

## 本地验收证据（2026-09-10）

- `npm run check:m1`：审阅修复后 407 项测试、四工作区类型检查、现有账号迁移契约、向导、Web／扩展生产构建和扩展安全检查通过。
- `npm run test:agent-runs`：14 项真实 Auth／PostgREST／SDK 旅程通过，新增不带模型或服务端密钥的历史发现、读取、取消及跨账号拒绝。
- `npm run test:planning-review`：真实本地账号 → 生产 Next 页面 → Server Action → RPC → 页面恢复通过。验证列表进入草案、两主题 1440／320 像素阅读、周安排和依赖、离线刷新恢复、取消后重新打开、定义修改后 stale 提示、正式蓝图仍为空及退出后的访问限制；无未捕获页面错误。
- 浏览器旅程通过真实 RPC 创建明确的测试草案；没有在 Web 中注入测试模型。SDK 运行能力由上一条独立验证，不把浏览器 fixture 称为实际推荐质量。
- `npm run test:e2e`：36 项生产回归通过，新增两类规划页面匿名访问保护。截图位于本地忽略目录 `.goal-loop/evidence/planning-review/`；这是 DOM 阅读工作台，不是正式 3D 美术验收。

最初浏览器测试在引入 AI SDK 时被 Playwright 的模块加载错误阻断，尚未执行任何测试旅程；没有为此改动生产依赖。随后分离 SDK 集成验证和浏览器测试数据准备，真实阅读／取消链路完整通过。测试结束退出会话并删除本次随机账号及关联记录，不发送邮件、不调用付费供应商。本批没有 SQL 迁移、托管变更、push 或用户浏览器接管。

复现命令可在 `scripts/with-m1-runtime.sh` 中运行；已安装 CLI 不在 PATH 时设置 `BLUEPRINT_SUPABASE_BIN`。本地配置工具拒绝非 `http://127.0.0.1:54321` 地址，不输出密钥。

独立审阅修复：规范轴发现成功刷新／取消没有完成状态播报，先用 React 语义断言复现，再保留持续挂载的状态区域，包含“状态未变但刷新成功”的消息；这不替代真实读屏器验收。需求轴发现合法长无空格标题的窄屏风险，真实浏览器确认详情页溢出后增加局部断词与可收缩规则，并保留普通中文和 240 字符连续英文两种回归。历史页本次探针未复现溢出，只做同范围防御处理。

最终普通／长文本两条真实账号浏览器旅程全部通过，两主题窄屏均无横向溢出；局部布局修复后生产 Web 重建和四工作区类型检查通过。两路复核各原 1 项 P2 已修复，剩余确认问题均为 0。本地两类临时账号查询剩余 0 个；原有账号及蓝图未被清理。
