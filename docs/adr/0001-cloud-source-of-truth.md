# Use a structured cloud Blueprint as the source of truth

Blueprint 3 replaces the local Markdown and Native Host runtime with one structured, versioned Blueprint per user in Supabase. Markdown remains a derived review and export format, the existing fixed extension identity is retained, and no compatibility or import layer is provided because the prototype has no real user data; Git tag `v2.0.0` preserves the previous implementation.
