{
  "name": "blueprint",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "An Agent-assisted personal goal system with a Web app and YouTube companion extension.",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @blueprint/web",
    "dev:extension": "npm run dev --workspace @blueprint/extension",
    "build:web": "npm run build --workspace @blueprint/web",
    "build:wxt": "npm run build --workspace @blueprint/extension",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test:m1": "vitest run",
    "test:db-contract": "node scripts/test-m1-migration.mjs",
    "test:e2e": "playwright test",
    "check:m1-wizard": "bash -n scripts/setup-m1-cloud.sh && node scripts/check-m1-wizard.mjs",
    "supabase:start": "bash scripts/with-m1-runtime.sh npx --yes supabase@2.115.0 start",
    "supabase:reset": "bash scripts/with-m1-runtime.sh npx --yes supabase@2.115.0 db reset",
    "supabase:test": "bash scripts/with-m1-runtime.sh npx --yes supabase@2.115.0 test db",
    "setup:m1-cloud": "bash scripts/with-m1-runtime.sh bash scripts/setup-m1-cloud.sh",
    "check:m1": "npm run typecheck && npm run test:m1 && npm run test:db-contract && npm run check:m1-wizard && npm run build:web && npm run build:wxt && node scripts/check-m1-extension.mjs",
    "test": "npm run test:m1",
    "check": "npm run check:m1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "@playwright/test": "1.62.1",
    "jsdom": "30.0.1",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
